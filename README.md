# taskmanagment
#  step 1 first go to root folder and copy backend env variable 
# step 2  create a .env file at root folder of backend after that paste over there
# step 3 now run the backend as npm run dev command in terminal

# step 4 copy env of frontend
# step 5 create a .env file at root folder of frontend after that paste over there
# step 6 now run as npm run dev
# now you can visit

# credentials
# Email: demo@gmail.com
# Password: 123456